package ion.content

import mindustry.content.Blocks.*
import mindustry.content.TechTree.*

import ion.content.*

object IonTechTree{
    fun load(){
        node(siliconSmelter){
            node(IonBlocks.brassSmelter)
        }
    }
}
